{
    IBClasses = (
        {
            ACTIONS = {cancel = id; ok = id; }; 
            CLASS = AccountEditPanel; 
            LANGUAGE = Java; 
            OUTLETS = {myComment = NSTextView; myName = NSTextField; myPanel = NSPanel; }; 
            SUPERCLASS = "java.lang.Object"; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}